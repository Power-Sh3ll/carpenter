[pytest]
testpaths = tests
# An exception escaping a background thread is reported as a warning rather than
# a failure, which is exactly backwards for a library whose whole job is running
# background threads: a monitor thread that dies takes every job in the registry
# with it. Promoting warnings to errors is what makes that a test failure.
filterwarnings =
    error
